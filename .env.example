# Microsoft Azure AD Application credentials
# Create an app registration in the Azure portal (https://portal.azure.com)
# and add these values from your app registration

# Application (client) ID from Azure AD app registration
REACT_APP_AZURE_CLIENT_ID=your-client-id-here

# Directory (tenant) ID from Azure AD app registration
# Use 'common' for multi-tenant apps or your specific tenant ID
REACT_APP_AZURE_TENANT_ID=your-tenant-id-or-common

# Server Configuration
# The base URL for the application (no trailing slash)
REACT_APP_BASE_URL=http://localhost:3000

# The port to run the development server on
PORT=3000

# API Configuration (if using a separate backend API)
REACT_APP_API_URL=http://localhost:8080/api

# Notification Configuration
# Default notification sound file (relative to public/sounds/)
REACT_APP_DEFAULT_NOTIFICATION_SOUND=bell-notification-337658.mp3
